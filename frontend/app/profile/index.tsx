import React from 'react';

// The Profile Index page is now handled by the _layout.tsx
// ensuring the ProfileMenu is always visible but animated.
// This component acts as a placeholder for the /profile route content (which is nothing, as the menu is in the layout)
export default function ProfileIndex() {
    return null;
}
